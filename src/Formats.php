<?php

declare(strict_types=1);

namespace Differ;

const SUPPORTED_FORMATS = [
    'json',
    'yml',
    'yaml',
];

const FORMAT_JSON = 'json';
const FORMAT_YML = 'yml';
const FORMAT_YAML = 'yaml';

const FORMAT_STYLISH = 'stylish';
const FORMAT_PLAIN = 'plain';
const FORMAT_JSON_OUTPUT = 'json';
